hls vs webrtc 카메라 1,2,4대일때 비교중. 내일 재실험필요( 지연율이 카메라 탭을 순차적으로 열고 로딩하고 있어서 ms가 누적됨, 탭당 1.5초씩 늦어지는 현상 발견, 페이지 로드 직후 HTML 내부에서부터 시간을 측정하는데 탭이 생성되는동안 브라우저 초기화 딜레이가 TTFF에 산입되어 수천 ms수준으로 기록됨. 크롬은 cpu 자원 절약을 위해 포커스가 없는 백그라운드 탭의 타이머. 네트워크 요청, 미디어 디코딩 속도를 강제로 제한한다. 먼저 생성된 카메라 탭들이 백그라운드로 밀려나면서 미디어 스트림을 수신하고도 디코더가 멈춰있다가 모든 탭이 생성되고나서야 활성화됨.
단일 카메라 측정시에는 백그라운드 대기 오버헤드나 스로틀링의 영향을 받지 않음. 
개선 사항은 각 카메라 탭을 열 때 미디어를 즉시 자동재생하지 않고 준비상태로 대기하고 모든 탭의 생성 및 로딩이 완료된 시점에 파이썬 스크립트가 동시 재생 트리거를 보내고 그때부터 TTFF를 측정한다.
동시 페이지 로딩 적용한다( 루프를 순차적으로 도는 대신 asyncio.gather를 사용해 모든 카메라 페이지를 동시에 열어 탭 로딩 오버해드를 줄인다.
 ai모듈에서 보내는 mqtt topic json형식 확인 및 공유 (완)
docker로 묶었는데 bat으로 했을때는 영상 나왔는데 지금은 안나옴
mqtt 해결 완 
통신경로, ai 분석 프로세스 정리
